import { Elysia } from 'elysia';

export const {{ fileName }}Middleware = new Elysia({ name: '{{ fileName }}-middleware' })
  .derive(({ request, headers, set }) => {
    // TODO: Implement middleware logic

    return {
      // TODO: Return middleware result
    };
  });
